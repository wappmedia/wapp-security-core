# Security

Do not submit customer data, live incident artifacts, credentials, malware
samples, quarantine bytes or production configuration to this public
repository.

Every contribution must pass the deterministic public-data boundary, the
package audit and the canonical Linux/macOS release matrix. Security-sensitive
changes require an independent exact-commit review before release.
